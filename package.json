{"scripts": {
    "build-ext": "cd extension && npm run compile && npm run package && vsce package --out ../build/",
    "start-server": "cd server && source venv/bin/activate && uvicorn main:app --reload"
}}